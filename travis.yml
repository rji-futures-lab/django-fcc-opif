dist: xenial
language: python
sudo: true
cache:
- pip
python:
- '3.7'
install:
- pip install pipenv --upgrade
- pipenv sync --dev
addons:
  postgresql: '10'
before_script:
- psql -c 'create database fcc-opif;' -U postgres
env:
  global:
  - PIPENV_VERBOSITY=-1
script:
- make test